[project]
name = "smolllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "transformers>=5.9.0",
    "torch>=2.0.0",
]
